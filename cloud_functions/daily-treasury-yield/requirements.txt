# Function dependencies, for example:
# package>=version
google-cloud-bigquery==3.17.2
google-cloud-bigquery-storage==2.24.0    # optional package, but without it, the slowdown of calling `.to_dataframe(...)` in the procedure `bq_client.query(sql).result().to_dataframe()` is 20x: https://github.com/googleapis/python-bigquery/issues/1492
pytz==2024.1
pyarrow==15.0.0
db-dtypes==1.2.0
pandas==1.3.5    # pandas >= 1.4 makes the treasury rate dataframe slow to use on the server (even though the server is built with 1.5.3); pandas >= 2 has unpickling issues for dataframes that were constructed with pandas < 2
fsspec
gcsfs
