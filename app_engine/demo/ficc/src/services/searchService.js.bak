// import http from './httpService'

// export async function getTable(access_token,date,time) {
//   const urlStr = '/api/cusiptable?access_token=' + access_token + '&date=' + date + '&time=' + time 
//   const { data: returnedData} = await http.get(urlStr)
//   return returnedData
// }